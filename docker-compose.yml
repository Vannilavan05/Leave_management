version: '3.8'

services:
  app:
    build: .
    image: leaveflow:latest
    ports:
      - "8000:8000"
    volumes:
      - ./data.json:/app/data.json
    environment:
      - PYTHONUNBUFFERED=1
    restart: unless-stopped
